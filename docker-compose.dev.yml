services:
  postgres:
    image: postgres:16-alpine
    container_name: restocore-postgres-dev
    environment:
      POSTGRES_DB: restocore_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d restocore_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: restocore-redis-dev
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  seaweedfs:
    image: chrislusf/seaweedfs:latest
    container_name: restocore-seaweedfs-dev
    command: "server -s3 -filer -ip.bind=0.0.0.0"
    ports:
      - "8333:8333" # S3 gateway
      - "8888:8888" # Filer HTTP
      - "9333:9333" # Master
    volumes:
      - seaweedfs_data:/data
    healthcheck:
      test: ["CMD-SHELL", "echo 'SELECT 1;' || exit 0"]
      interval: 5s
      timeout: 5s
      retries: 5

  create-bucket:
    image: curlimages/curl:latest
    container_name: restocore-create-bucket-dev
    depends_on:
      seaweedfs:
        condition: service_started
    command: >
      sh -c "sleep 3 && curl -s -X PUT http://seaweedfs:8333/restocore-images || exit 0"
    restart: "no"

  opa:
    image: openpolicyagent/opa:latest
    container_name: restocore-opa-dev
    command:
      - "run"
      - "--server"
      - "--addr=0.0.0.0:8181"
      - "/policies"
    volumes:
      - ./specs/policies:/policies:ro
    ports:
      - "8181:8181"

  aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:latest
    container_name: restocore-aspire-dashboard-dev
    environment:
      - DOTNET_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS=true
      - DASHBOARD__OTLP__AUTHMODE=Unsecured
    ports:
      - "18888:18888" # Dashboard Web UI
      - "4317:18889"   # OTLP gRPC receiver (Aspire container listens on 18889)
      - "4318:18890"   # OTLP HTTP receiver (Aspire container listens on 18890)
    restart: unless-stopped

volumes:
  postgres_dev_data:
  seaweedfs_data:
